---
version: '3.2'
services:

  ut:
    build:
      context: ./ui
    entrypoint:
      - npm
      - run
      - test

  apiut:
    build:
      context: ./api
    command:
      - go
      - test
      - ./tests
